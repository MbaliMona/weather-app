<!DOCTYPE html>
<html>
    <head>
        <title>{{title}}</title>
        <link rel="stylesheet" href="/css/styles.css"/> 
    </head>
    <body>
         <div class="main-content">
            {{>header}}
            <div class="wrap">
  <div class="404">
    <pre><code>
      (<span class="blue">function</span>( 404,res ) {
           <span class="blue">function</span> launch() {
              <span class="blue">if</span>( inDevelopment({Backend}) ) {
                  <span class="blue">return</span>;
              }
              <span class="comment">//you on the wrong page</span>
              <span class="comment">//If you looking for the weather app type localhost:3000</span>
        }
      window.addEventListner( <span class="load">'load'</span>, launch );
      }( <span class="blue">true</span> ));
    </code></pre>
  </div>
</div>
        </div>
    </body>
    <style>
        @import url(https://fonts.googleapis.com/css?family=Inconsolata);
html,
body {
  background: rgb(22, 22, 22);
  font-family: 'Inconsolata', sans-serif;
}
.wrap {
  top: 50%;
  left: 50%;
  width: 400px;
  height: 200px;
  margin-left: -200px;
  margin-top: -100px;
  position: absolute;
}
code {
  color: white;
}
span.blue {
  color: #48beef;
}
span.comment {
  color: #7f8c8d;
}
span.load {
  color: #f39c12;
}

    </style>
</html>